import { createSlice } from '@reduxjs/toolkit';

export const shopppingCartSlice = createSlice({
    name: 'shoppingCart',
    initialState: {
        quantity: 0,
        product: []
    },
    reducers: {

    }
});
